source "https://rubygems.org"

# Bundle edge Rails instead: gem "rails", github: "rails/rails", branch: "main"
gem "rails", "~> 8.1.3", ">= 8.1.3.1"
# Use sqlite3 as the database for Active Record
gem "sqlite3", ">= 2.1"
# Use the Puma web server [https://github.com/puma/puma]
gem "puma", ">= 5.0"
# Build JSON APIs with ease [https://github.com/rails/jbuilder]
# gem "jbuilder"

# Use Active Model has_secure_password [https://guides.rubyonrails.org/active_model_basics.html#securepassword]
# gem "bcrypt", "~> 3.1.7"

# Windows does not include zoneinfo files, so bundle the tzinfo-data gem
gem "tzinfo-data", platforms: %i[ windows jruby ]

# Use the database-backed adapters for Rails.cache, Active Job, and Action Cable
gem "solid_cache"
gem "solid_queue"
gem "solid_cable"

# Reduces boot times through caching; required in config/boot.rb
gem "bootsnap", require: false

# Add HTTP asset caching/compression and X-Sendfile acceleration to Puma [https://github.com/basecamp/thruster/]
gem "thruster", require: false

# Active Storage variants are not used: there are no uploads in scope.
# gem "image_processing", "~> 1.2"

# CORS is deliberately absent. Vite proxies /api in development and Rails
# serves the built bundle in production, so every request is same-origin.
# See docs/decisions.md 6.
# gem "rack-cors"

# Pinned. json 3.0 made JSON.parse's options keyword-only:
#
#   def parse(source, on_load: nil, object_class: nil, array_class: nil, **options)
#
# Rails 8.1 still passes a positional options hash when deserializing a json
# column, so every read of audit_events.metadata raises ArgumentError
# "wrong number of arguments (given 2, expected 1)". Unpin once Rails ships
# a release that calls it with keywords.
gem "json", "~> 2.9"

# Serialization. Chosen over jbuilder because resources are plain classes,
# which are faster to test than templates.
gem "alba"

# Pagination for the employee directory.
gem "pagy"

group :development, :test do
  # See https://guides.rubyonrails.org/debugging_rails_applications.html#debugging-with-the-debug-gem
  gem "debug", platforms: %i[ mri windows ], require: "debug/prelude"

  # Audits gems for known security defects (use config/bundler-audit.yml to ignore issues)
  gem "bundler-audit", require: false

  # Static analysis for security vulnerabilities [https://brakemanscanner.org/]
  gem "brakeman", require: false

  # Omakase Ruby styling [https://github.com/rails/rubocop-rails-omakase/]
  gem "rubocop-rails-omakase", require: false

  # Specs. rspec-rails and factory_bot_rails are here rather than in :test
  # alone so their generators are available.
  gem "rspec-rails"
  gem "factory_bot_rails"

  # Faker is also used by db/seeds.rb, which runs in development.
  # Seeded with a fixed value there: the dataset must be reproducible.
  gem "faker"
end

group :test do
  gem "shoulda-matchers"
  gem "simplecov", require: false
end
